# __APP_TITLE__

This is a generated uni-app scaffold for a `__APP_TYPE__` mini program.

## What it includes

- `appKey`-driven runtime config loading
- platform adapter entry points
- a minimal home page
- separate `wechat` / `douyin` / `xiaohongshu` build scripts

## Next steps

1. Add your business pages under `src/pages`
2. Fill the platform config records in admin
3. Inject platform appid / secrets through build-time secrets
