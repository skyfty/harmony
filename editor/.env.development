# Local development server API base
# VITE_SERVER_API_BASE_URL=http://localhost:4000
VITE_SERVER_API_BASE_URL=https://cdn.touchmagic.cn

# Optional prefix in front of server routes (defaults to /api when omitted)
VITE_SERVER_API_PREFIX=/api
