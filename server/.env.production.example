# 生产环境配置示例，请复制为 .env.production 并修改敏感值
NODE_ENV=production
# 服务端口（Koa），默认 4000，与容器 EXPOSE 一致
PORT=4000

# MongoDB 连接串：使用 docker-compose 内部服务名与默认容器端口
# 注意：虽然宿主机映射为 27018，但容器之间连通仍用 27017
MONGODB_URI=mongodb://mongo:27017/harmony

# 请务必修改为强随机字符串
JWT_SECRET=please-change-me

# 上传文件的存储与公网 URL 映射
ASSET_STORAGE_PATH=/app/uploads
ASSET_PUBLIC_URL=https://cdn.touchmagic.cn/uploads

# 跨域允许的编辑器站点（如有 Web 前端），可留空
EDITOR_PUBLIC_URL=https://editor.example.com
