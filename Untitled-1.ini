D:\harmony\editor\src\views\ScenePreviewView.vue中的loadActualAssetForPlaceholder函数在调用到prepareInstancedNodesForDocument函数中的removeNodeSubtree函数会将当前已经注册给previewNodeMap的节点释放掉，
但是在随后的调用registerSubtree之前并没有重新向previewNodeMap注册这些节点，导致在调用resolveNodeById时无法找到对应的节点，进而引发错误。修复这个问题。 但是要仔细分析代码逻辑，以避免潜在的错误。

D:\harmony\miniprogram\src\pages\scene-viewer\index.vue中由InstancedMesh创建的节点在进行场景无法通过intersectObjects来进行拾取，主要体现在handleBehaviorClick函数中在点击场景中的InstancedMesh节点时无法正确拾取到对应的节点。需要修复这个问题，使得InstancedMesh节点能够被正确拾取。