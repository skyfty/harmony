重新实现/home/sky/harmony/schema/physicsEngine.ts中resolveWallShape函数，为wall墙体节点网格生成刚体错误，使用网格几何体（trimesh）来生成刚体。这样可以确保刚体的位置和形状与墙体网格完全对齐，能够更准确地反映墙体的实际几何形状，从而提升物理模拟的准确性和稳定性。要考虑到墙体的宽度，和墙体的开口形状，和墙体的拐角处处理。

墙体网格生成的不对， 首先位置不对， 没有和墙体网格对齐， 其次形状不对， 不能正确反映墙体的实际形状。现在是一个闭合的盒子， 但是墙体网格是开口的。还需要考虑墙体的宽度， 不能直接用包围盒。如图所示白色为墙体，黄色线为刚体。深色为地面。可以参考road路面的刚体生成逻辑，使用网格几何体（trimesh）来生成刚体。这样可以确保刚体的位置和形状与墙体网格完全对齐，能够更准确地反映墙体的实际几何形状，从而提升物理模拟的准确性和稳定性。

刚体的碰撞网格生成是正确的， 但是是墙体的碰撞网格调试线显示的不对， 和碰撞网格不匹配。如图，黄色线为墙体的碰撞网格调试线， 和实际墙体网格不匹配。需要调整墙体碰撞网格调试线的生成逻辑， 使其和实际墙体网格对齐。主要是没有考虑墙体的宽度， 还有墙体的开口形状， 和墙体的拐角处处理。

在场景中渲染墙体时，如果在场景中的墙体组件中设置了wall body model属性，则使用该模型来生成墙体的可视展示，否则使用默认的墙体模型进行渲染。墙体的模型网格网格使用InstancedMesh进行渲染，以提升渲染性能。只需要原样使用墙体组件中的wall body model属性来生成墙体的可视展示部分即可。不需要参考墙体的宽度、高度、和厚度属性来生成墙体的可视展示部分InstancedMesh。但是InstancedMesh的位置和旋转需要和墙体中心线对齐。InstancedMesh不需要有间隔， 直接紧密排列即可。用于实现用一个单独的墙垛网格模型来渲染整个墙体的效果。
在墙体的连接处， 如果同时设置了Wall joint model属性， 则使用该模型来生成墙体连接处的可视展示，否则使用默认的墙体连接处模型进行渲染。墙体连接处的模型网格同样使用InstancedMesh进行渲染，以提升渲染性能。墙体连接处的InstancedMesh的位置和旋转需要和墙体连接处对齐。墙体连接处的InstancedMesh同墙体的InstancedMesh同样不需要有间隔， 直接紧密排列即可。

在/home/sky/harmony/editor/src/components/editor/SceneViewport.vue中实现墙体的可视展示逻辑， 使用墙体组件中的wall body model属性和wall joint model属性来生成墙体和墙体连接处的可视展示部分。使用InstancedMesh来渲染墙体和墙体连接处的模型网格，以提升渲染性能。确保InstancedMesh的位置和旋转与墙体中心线和连接处对齐。现在只在/home/sky/harmony/editor/src/views/ScenePreviewView.vue中实现了墙体的可视展示逻辑， 需要确保在编辑器场景视口中也能正确显示墙体的可视效果。

为/home/sky/harmony/editor/src/components/inspector/WallPanel.vue

现在使用墙体组件中的wall body model属性和wall joint model属性来生成墙体和墙体连接处的可视展示部分。使用InstancedMesh来渲染墙体和墙体连接处的模型网格，以提升渲染性能。确保InstancedMesh的位置和旋转与墙体中心线和连接处对齐。现在/home/sky/harmony/editor/src/views/ScenePreviewView.vue中无法渲染通过模型设置的墙体了，修复这个问题 

第346行，object.userData?.wallInstancingDebug为undefined，导致无法进入if语句块，无法渲染墙体的可视展示部分。需要检查wallInstancingDebug属性的设置逻辑，确保在需要渲染墙体可视展示时正确设置该属性。

在/home/sky/harmony/editor/src/components/editor/SceneViewport.vue中依然没有使用墙体组件中的wall body model属性和wall joint model属性来生成墙体和墙体连接处的可视展示部分。需要补充这部分逻辑，确保在编辑器场景视口中也能正确显示墙体的可视效果。

依然没有显示bodyAssetId指定的模型，但是在 /home/sky/harmony/editor/src/views/ScenePreviewView.vue中是显示正常的， 在/home/sky/harmony/editor/src/components/editor/SceneViewport.vue中只显示了默认的墙体模型。